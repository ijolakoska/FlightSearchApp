export class SearchFlight {
    source: string; //instead of strings we could use the Airport class
    destination: string;
    numAdults: number;
    numChildren: number;    
    numInfants: number;
    departureDate: Date;
    arrivalDate: Date;
    seatingClass: string;
    counter: number;
}